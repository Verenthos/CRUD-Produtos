export interface Product {
    id: number;
    name: string;
    quantity: number;
    price: number;
  }
  
  export interface ProductInput {
    name: string;
    quantity: number;
    price: number;
  }